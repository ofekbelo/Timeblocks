# Sub-Agent 3.3: UI Components

**Parent:** Agent 3 - Frontend Foundation  
**Status:** Ready to Execute  
**Dependencies:** Sub-Agent 3.1, Sub-Agent 3.2  
**Estimated Time:** 3-4 hours  
**Working Directory:** `timeblocks/apps/web/`

---

## 📋 Mission

Build a complete design system with SCSS and create a library of reusable custom UI components (no external UI library!).

---

## 🎯 What You'll Build

1. **SCSS Design System** - Variables, mixins, utilities
2. **Core UI Components** - Button, Input, Card, Modal, etc.
3. **Form Components** - FormField, FormError
4. **Utility Components** - Spinner, Avatar, Badge
5. **Professional Styling** - Consistent, beautiful, accessible

---

## ✅ Step-by-Step Tasks

### TASK 1: Create Design System Structure

Create directories:

```bash
mkdir -p src/styles src/components/ui
```

---

### TASK 2: Create SCSS Variables

Create `src/styles/_variables.scss`:

```scss
// ==========================================
// COLORS
// ==========================================

// Primary
$color-primary: #4a90e2;
$color-primary-dark: #3a7bc8;
$color-primary-light: #6ba3e8;

// Secondary
$color-secondary: #667eea;
$color-secondary-dark: #5568d3;
$color-secondary-light: #7c8ef0;

// Status Colors
$color-success: #7ed321;
$color-success-dark: #6bb91c;
$color-success-light: #92dd3c;

$color-warning: #f5a623;
$color-warning-dark: #db941f;
$color-warning-light: #f7b851;

$color-danger: #d0021b;
$color-danger-dark: #b80118;
$color-danger-light: #e03347;

$color-info: #4a90e2;
$color-info-dark: #3a7bc8;
$color-info-light: #6ba3e8;

// Grays
$color-gray-50: #f9fafb;
$color-gray-100: #f3f4f6;
$color-gray-200: #e5e7eb;
$color-gray-300: #d1d5db;
$color-gray-400: #9ca3af;
$color-gray-500: #6b7280;
$color-gray-600: #4b5563;
$color-gray-700: #374151;
$color-gray-800: #1f2937;
$color-gray-900: #111827;

// Dark Theme
$color-bg-dark: #1a202c;
$color-bg-dark-secondary: #2d3748;

// ==========================================
// SPACING
// ==========================================

$spacing-xs: 0.25rem; // 4px
$spacing-sm: 0.5rem; // 8px
$spacing-md: 1rem; // 16px
$spacing-lg: 1.5rem; // 24px
$spacing-xl: 2rem; // 32px
$spacing-2xl: 3rem; // 48px
$spacing-3xl: 4rem; // 64px

// ==========================================
// TYPOGRAPHY
// ==========================================

$font-family-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto',
  'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
  sans-serif;
$font-family-mono: 'SF Mono', 'Monaco', 'Inconsolata', 'Fira Mono',
  'Droid Sans Mono', 'Source Code Pro', monospace;

$font-size-xs: 0.75rem; // 12px
$font-size-sm: 0.875rem; // 14px
$font-size-base: 1rem; // 16px
$font-size-lg: 1.125rem; // 18px
$font-size-xl: 1.25rem; // 20px
$font-size-2xl: 1.5rem; // 24px
$font-size-3xl: 1.875rem; // 30px
$font-size-4xl: 2.25rem; // 36px

$font-weight-light: 300;
$font-weight-normal: 400;
$font-weight-medium: 500;
$font-weight-semibold: 600;
$font-weight-bold: 700;

$line-height-tight: 1.25;
$line-height-normal: 1.5;
$line-height-relaxed: 1.75;

// ==========================================
// BORDERS
// ==========================================

$border-radius-sm: 0.25rem; // 4px
$border-radius-md: 0.5rem; // 8px
$border-radius-lg: 0.75rem; // 12px
$border-radius-xl: 1rem; // 16px
$border-radius-2xl: 1.5rem; // 24px
$border-radius-full: 9999px;

$border-width-thin: 1px;
$border-width-medium: 2px;
$border-width-thick: 4px;

// ==========================================
// SHADOWS
// ==========================================

$shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
$shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
  0 2px 4px -1px rgba(0, 0, 0, 0.06);
$shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
  0 4px 6px -2px rgba(0, 0, 0, 0.05);
$shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1),
  0 10px 10px -5px rgba(0, 0, 0, 0.04);
$shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.25);

// ==========================================
// Z-INDEX
// ==========================================

$z-index-dropdown: 1000;
$z-index-sticky: 1020;
$z-index-fixed: 1030;
$z-index-modal-backdrop: 1040;
$z-index-modal: 1050;
$z-index-popover: 1060;
$z-index-tooltip: 1070;

// ==========================================
// TRANSITIONS
// ==========================================

$transition-fast: 150ms ease;
$transition-base: 200ms ease;
$transition-slow: 300ms ease;

// ==========================================
// BREAKPOINTS
// ==========================================

$breakpoint-sm: 640px;
$breakpoint-md: 768px;
$breakpoint-lg: 1024px;
$breakpoint-xl: 1280px;
$breakpoint-2xl: 1536px;
```

---

### TASK 3: Create SCSS Mixins

Create `src/styles/_mixins.scss`:

```scss
// ==========================================
// FLEXBOX UTILITIES
// ==========================================

@mixin flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}

@mixin flex-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

@mixin flex-column {
  display: flex;
  flex-direction: column;
}

// ==========================================
// RESPONSIVE
// ==========================================

@mixin respond-to($breakpoint) {
  @if $breakpoint == 'sm' {
    @media (min-width: #{$breakpoint-sm}) {
      @content;
    }
  } @else if $breakpoint == 'md' {
    @media (min-width: #{$breakpoint-md}) {
      @content;
    }
  } @else if $breakpoint == 'lg' {
    @media (min-width: #{$breakpoint-lg}) {
      @content;
    }
  } @else if $breakpoint == 'xl' {
    @media (min-width: #{$breakpoint-xl}) {
      @content;
    }
  }
}

// ==========================================
// BUTTON RESET
// ==========================================

@mixin button-reset {
  background: none;
  border: none;
  padding: 0;
  cursor: pointer;
  font-family: inherit;
  font-size: inherit;
  color: inherit;
}

// ==========================================
// INPUT RESET
// ==========================================

@mixin input-reset {
  appearance: none;
  background: none;
  border: none;
  outline: none;
  font-family: inherit;
  font-size: inherit;
  color: inherit;
}

// ==========================================
// TRUNCATE TEXT
// ==========================================

@mixin truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

// ==========================================
// SPINNER ANIMATION
// ==========================================

@mixin spinner($size: 1.5rem, $color: currentColor) {
  width: $size;
  height: $size;
  border: 2px solid rgba($color, 0.2);
  border-top-color: $color;
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

// ==========================================
// FADE IN ANIMATION
// ==========================================

@mixin fade-in($duration: 0.3s) {
  animation: fadeIn $duration ease;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}
```

---

### TASK 4: Create Reset Styles

Create `src/styles/_reset.scss`:

```scss
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-size: 16px;
}

body {
  font-family: $font-family-sans;
  font-size: $font-size-base;
  line-height: $line-height-normal;
  color: $color-gray-900;
  background-color: $color-gray-50;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

h1,
h2,
h3,
h4,
h5,
h6 {
  margin: 0;
  font-weight: $font-weight-semibold;
}

button {
  font-family: inherit;
}

a {
  color: inherit;
  text-decoration: none;
}

img {
  max-width: 100%;
  height: auto;
}
```

---

### TASK 5: Update Global Styles

Update `src/styles/global.scss`:

```scss
@import 'variables';
@import 'mixins';
@import 'reset';
```

---

### TASK 6: Create Button Component

Create `src/components/ui/Button/Button.tsx`:

```typescript
import { ButtonHTMLAttributes, ReactNode } from 'react';
import styles from './Button.module.scss';

export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  children: ReactNode;
  variant?: 'primary' | 'secondary' | 'success' | 'danger' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  fullWidth?: boolean;
  isLoading?: boolean;
}

export function Button({
  children,
  variant = 'primary',
  size = 'md',
  fullWidth = false,
  isLoading = false,
  disabled,
  className,
  ...props
}: ButtonProps) {
  const classes = [
    styles.button,
    styles[variant],
    styles[size],
    fullWidth && styles.fullWidth,
    isLoading && styles.loading,
    className,
  ]
    .filter(Boolean)
    .join(' ');

  return (
    <button className={classes} disabled={disabled || isLoading} {...props}>
      {isLoading && <span className={styles.spinner} />}
      <span className={styles.content}>{children}</span>
    </button>
  );
}
```

Create `src/components/ui/Button/Button.module.scss`:

```scss
@import '../../../styles/variables';
@import '../../../styles/mixins';

.button {
  @include flex-center;
  gap: $spacing-sm;
  border: none;
  border-radius: $border-radius-md;
  font-weight: $font-weight-semibold;
  cursor: pointer;
  transition: all $transition-base;
  position: relative;

  &:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }

  &.sm {
    padding: $spacing-sm $spacing-md;
    font-size: $font-size-sm;
  }

  &.md {
    padding: 0.625rem $spacing-lg;
    font-size: $font-size-base;
  }

  &.lg {
    padding: $spacing-md $spacing-xl;
    font-size: $font-size-lg;
  }

  &.fullWidth {
    width: 100%;
  }

  // Variants
  &.primary {
    background-color: $color-primary;
    color: white;

    &:hover:not(:disabled) {
      background-color: $color-primary-dark;
      transform: translateY(-1px);
      box-shadow: $shadow-md;
    }

    &:active:not(:disabled) {
      transform: translateY(0);
    }
  }

  &.secondary {
    background-color: $color-gray-200;
    color: $color-gray-800;

    &:hover:not(:disabled) {
      background-color: $color-gray-300;
    }
  }

  &.success {
    background-color: $color-success;
    color: white;

    &:hover:not(:disabled) {
      background-color: $color-success-dark;
    }
  }

  &.danger {
    background-color: $color-danger;
    color: white;

    &:hover:not(:disabled) {
      background-color: $color-danger-dark;
    }
  }

  &.ghost {
    background-color: transparent;
    color: $color-primary;

    &:hover:not(:disabled) {
      background-color: rgba($color-primary, 0.1);
    }
  }

  // Loading state
  &.loading {
    .content {
      opacity: 0.6;
    }
  }
}

.spinner {
  @include spinner(1rem);
  position: absolute;
}

.content {
  display: flex;
  align-items: center;
  gap: $spacing-sm;
}
```

Create `src/components/ui/Button/index.ts`:

```typescript
export { Button } from './Button';
export type { ButtonProps } from './Button';
```

---

### TASK 7: Create Input Component

Create `src/components/ui/Input/Input.tsx`:

```typescript
import { InputHTMLAttributes, ReactNode, forwardRef } from 'react';
import styles from './Input.module.scss';

export interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
  label?: string;
  error?: string;
  helperText?: string;
  leftIcon?: ReactNode;
  rightIcon?: ReactNode;
  fullWidth?: boolean;
}

export const Input = forwardRef<HTMLInputElement, InputProps>(
  (
    {
      label,
      error,
      helperText,
      leftIcon,
      rightIcon,
      fullWidth = false,
      className,
      id,
      ...props
    },
    ref
  ) => {
    const inputId = id || `input-${Math.random().toString(36).substr(2, 9)}`;

    const wrapperClasses = [
      styles.wrapper,
      fullWidth && styles.fullWidth,
      error && styles.hasError,
      className,
    ]
      .filter(Boolean)
      .join(' ');

    return (
      <div className={wrapperClasses}>
        {label && (
          <label htmlFor={inputId} className={styles.label}>
            {label}
          </label>
        )}

        <div className={styles.inputWrapper}>
          {leftIcon && <span className={styles.leftIcon}>{leftIcon}</span>}
          <input
            ref={ref}
            id={inputId}
            className={styles.input}
            {...props}
          />
          {rightIcon && <span className={styles.rightIcon}>{rightIcon}</span>}
        </div>

        {error && <span className={styles.error}>{error}</span>}
        {!error && helperText && (
          <span className={styles.helperText}>{helperText}</span>
        )}
      </div>
    );
  }
);

Input.displayName = 'Input';
```

Create `src/components/ui/Input/Input.module.scss`:

```scss
@import '../../../styles/variables';
@import '../../../styles/mixins';

.wrapper {
  display: flex;
  flex-direction: column;
  gap: $spacing-xs;

  &.fullWidth {
    width: 100%;
  }
}

.label {
  font-size: $font-size-sm;
  font-weight: $font-weight-medium;
  color: $color-gray-700;
}

.inputWrapper {
  position: relative;
  display: flex;
  align-items: center;
}

.input {
  width: 100%;
  padding: 0.625rem $spacing-md;
  border: $border-width-medium solid $color-gray-300;
  border-radius: $border-radius-md;
  font-size: $font-size-base;
  color: $color-gray-900;
  background-color: white;
  transition: all $transition-base;

  &:focus {
    outline: none;
    border-color: $color-primary;
    box-shadow: 0 0 0 3px rgba($color-primary, 0.1);
  }

  &:disabled {
    background-color: $color-gray-100;
    cursor: not-allowed;
    opacity: 0.6;
  }

  .hasError & {
    border-color: $color-danger;

    &:focus {
      box-shadow: 0 0 0 3px rgba($color-danger, 0.1);
    }
  }

  // Adjust padding when icons present
  .leftIcon ~ & {
    padding-left: 2.75rem;
  }

  .rightIcon ~ & {
    padding-right: 2.75rem;
  }
}

.leftIcon,
.rightIcon {
  position: absolute;
  color: $color-gray-500;
  display: flex;
  align-items: center;
}

.leftIcon {
  left: $spacing-md;
}

.rightIcon {
  right: $spacing-md;
}

.error {
  font-size: $font-size-sm;
  color: $color-danger;
}

.helperText {
  font-size: $font-size-sm;
  color: $color-gray-600;
}
```

Create `src/components/ui/Input/index.ts`:

```typescript
export { Input } from './Input';
export type { InputProps } from './Input';
```

---

### TASK 8: Create Card Component

Create `src/components/ui/Card/Card.tsx`:

```typescript
import { ReactNode, HTMLAttributes } from 'react';
import styles from './Card.module.scss';

export interface CardProps extends HTMLAttributes<HTMLDivElement> {
  children: ReactNode;
  variant?: 'default' | 'outlined' | 'elevated';
  padding?: 'none' | 'sm' | 'md' | 'lg';
}

export function Card({
  children,
  variant = 'default',
  padding = 'md',
  className,
  ...props
}: CardProps) {
  const classes = [
    styles.card,
    styles[variant],
    styles[`padding-${padding}`],
    className,
  ]
    .filter(Boolean)
    .join(' ');

  return (
    <div className={classes} {...props}>
      {children}
    </div>
  );
}
```

Create `src/components/ui/Card/Card.module.scss`:

```scss
@import '../../../styles/variables';
@import '../../../styles/mixins';

.card {
  background: white;
  border-radius: $border-radius-lg;
  transition: all $transition-base;

  &.default {
    border: $border-width-thin solid $color-gray-200;
  }

  &.outlined {
    border: $border-width-medium solid $color-gray-300;
  }

  &.elevated {
    box-shadow: $shadow-md;

    &:hover {
      box-shadow: $shadow-lg;
      transform: translateY(-2px);
    }
  }

  &.padding-none {
    padding: 0;
  }

  &.padding-sm {
    padding: $spacing-md;
  }

  &.padding-md {
    padding: $spacing-lg;
  }

  &.padding-lg {
    padding: $spacing-xl;
  }
}
```

Create `src/components/ui/Card/index.ts`:

```typescript
export { Card } from './Card';
export type { CardProps } from './Card';
```

---

### TASK 9: Create Modal Component

Create `src/components/ui/Modal/Modal.tsx`:

```typescript
import { ReactNode, useEffect } from 'react';
import styles from './Modal.module.scss';

export interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title?: string;
  children: ReactNode;
  size?: 'sm' | 'md' | 'lg' | 'xl';
}

export function Modal({
  isOpen,
  onClose,
  title,
  children,
  size = 'md',
}: ModalProps) {
  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden';
    } else {
      document.body.style.overflow = 'unset';
    }

    return () => {
      document.body.style.overflow = 'unset';
    };
  }, [isOpen]);

  useEffect(() => {
    const handleEscape = (e: KeyboardEvent) => {
      if (e.key === 'Escape') {
        onClose();
      }
    };

    if (isOpen) {
      window.addEventListener('keydown', handleEscape);
    }

    return () => {
      window.removeEventListener('keydown', handleEscape);
    };
  }, [isOpen, onClose]);

  if (!isOpen) return null;

  return (
    <div className={styles.overlay} onClick={onClose}>
      <div
        className={`${styles.modal} ${styles[size]}`}
        onClick={(e) => e.stopPropagation()}
      >
        {title && (
          <div className={styles.header}>
            <h2 className={styles.title}>{title}</h2>
            <button className={styles.closeBtn} onClick={onClose}>
              ✕
            </button>
          </div>
        )}

        <div className={styles.content}>{children}</div>
      </div>
    </div>
  );
}
```

Create `src/components/ui/Modal/Modal.module.scss`:

```scss
@import '../../../styles/variables';
@import '../../../styles/mixins';

.overlay {
  position: fixed;
  inset: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: $spacing-lg;
  z-index: $z-index-modal;
  @include fade-in;
}

.modal {
  background: white;
  border-radius: $border-radius-lg;
  box-shadow: $shadow-2xl;
  max-height: 90vh;
  overflow: hidden;
  display: flex;
  flex-direction: column;

  &.sm {
    width: 100%;
    max-width: 400px;
  }

  &.md {
    width: 100%;
    max-width: 600px;
  }

  &.lg {
    width: 100%;
    max-width: 800px;
  }

  &.xl {
    width: 100%;
    max-width: 1000px;
  }
}

.header {
  @include flex-between;
  padding: $spacing-lg;
  border-bottom: $border-width-thin solid $color-gray-200;
}

.title {
  font-size: $font-size-xl;
  font-weight: $font-weight-semibold;
  color: $color-gray-900;
}

.closeBtn {
  @include button-reset;
  width: 2rem;
  height: 2rem;
  @include flex-center;
  border-radius: $border-radius-md;
  color: $color-gray-500;
  transition: all $transition-base;

  &:hover {
    background-color: $color-gray-100;
    color: $color-gray-900;
  }
}

.content {
  padding: $spacing-lg;
  overflow-y: auto;
}
```

Create `src/components/ui/Modal/index.ts`:

```typescript
export { Modal } from './Modal';
export type { ModalProps } from './Modal';
```

---

### TASK 10: Create Spinner Component

Create `src/components/ui/Spinner/Spinner.tsx`:

```typescript
import styles from './Spinner.module.scss';

export interface SpinnerProps {
  size?: 'sm' | 'md' | 'lg';
  color?: 'primary' | 'white' | 'gray';
}

export function Spinner({ size = 'md', color = 'primary' }: SpinnerProps) {
  return (
    <div className={`${styles.spinner} ${styles[size]} ${styles[color]}`} />
  );
}
```

Create `src/components/ui/Spinner/Spinner.module.scss`:

```scss
@import '../../../styles/variables';
@import '../../../styles/mixins';

.spinner {
  border-radius: 50%;
  animation: spin 0.6s linear infinite;

  &.sm {
    width: 1rem;
    height: 1rem;
    border-width: 2px;
  }

  &.md {
    width: 1.5rem;
    height: 1.5rem;
    border-width: 2px;
  }

  &.lg {
    width: 2.5rem;
    height: 2.5rem;
    border-width: 3px;
  }

  &.primary {
    border: 2px solid rgba($color-primary, 0.2);
    border-top-color: $color-primary;
  }

  &.white {
    border: 2px solid rgba(white, 0.2);
    border-top-color: white;
  }

  &.gray {
    border: 2px solid rgba($color-gray-400, 0.2);
    border-top-color: $color-gray-400;
  }
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}
```

Create `src/components/ui/Spinner/index.ts`:

```typescript
export { Spinner } from './Spinner';
export type { SpinnerProps } from './Spinner';
```

---

### TASK 11: Create Badge Component

Create `src/components/ui/Badge/Badge.tsx`:

```typescript
import { ReactNode } from 'react';
import styles from './Badge.module.scss';

export interface BadgeProps {
  children: ReactNode;
  variant?: 'primary' | 'success' | 'warning' | 'danger' | 'gray';
  size?: 'sm' | 'md' | 'lg';
}

export function Badge({ children, variant = 'primary', size = 'md' }: BadgeProps) {
  return (
    <span className={`${styles.badge} ${styles[variant]} ${styles[size]}`}>
      {children}
    </span>
  );
}
```

Create `src/components/ui/Badge/Badge.module.scss`:

```scss
@import '../../../styles/variables';

.badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-weight: $font-weight-medium;
  border-radius: $border-radius-full;
  white-space: nowrap;

  &.sm {
    padding: 0.125rem 0.5rem;
    font-size: $font-size-xs;
  }

  &.md {
    padding: 0.25rem 0.75rem;
    font-size: $font-size-sm;
  }

  &.lg {
    padding: 0.375rem 1rem;
    font-size: $font-size-base;
  }

  &.primary {
    background-color: rgba($color-primary, 0.1);
    color: $color-primary-dark;
  }

  &.success {
    background-color: rgba($color-success, 0.1);
    color: $color-success-dark;
  }

  &.warning {
    background-color: rgba($color-warning, 0.1);
    color: $color-warning-dark;
  }

  &.danger {
    background-color: rgba($color-danger, 0.1);
    color: $color-danger-dark;
  }

  &.gray {
    background-color: $color-gray-200;
    color: $color-gray-700;
  }
}
```

Create `src/components/ui/Badge/index.ts`:

```typescript
export { Badge } from './Badge';
export type { BadgeProps } from './Badge';
```

---

### TASK 12: Create UI Components Index

Create `src/components/ui/index.ts`:

```typescript
export * from './Button';
export * from './Input';
export * from './Card';
export * from './Modal';
export * from './Spinner';
export * from './Badge';
```

---

### TASK 13: Update Dashboard to Use Components

Update `src/pages/Dashboard/Dashboard.tsx`:

```typescript
import { Card } from '@/components/ui/Card';
import { Button } from '@/components/ui/Button';
import { Badge } from '@/components/ui/Badge';
import { useAppSelector } from '@/store/hooks';
import styles from './Dashboard.module.scss';

export function Dashboard() {
  const user = useAppSelector((state) => state.auth.user);

  return (
    <div className={styles.dashboard}>
      <div className={styles.header}>
        <div>
          <h1>Welcome back, {user?.fullName || 'User'}!</h1>
          <p>Here's what's happening with your time tracking today.</p>
        </div>
        <Button variant="primary">Start Timer</Button>
      </div>

      <div className={styles.grid}>
        <Card variant="elevated">
          <div className={styles.stat}>
            <div className={styles.statValue}>0h 00m</div>
            <div className={styles.statLabel}>Today</div>
          </div>
        </Card>

        <Card variant="elevated">
          <div className={styles.stat}>
            <div className={styles.statValue}>0h 00m</div>
            <div className={styles.statLabel}>This Week</div>
          </div>
        </Card>

        <Card variant="elevated">
          <div className={styles.stat}>
            <div className={styles.statValue}>0</div>
            <div className={styles.statLabel}>Projects</div>
          </div>
        </Card>

        <Card variant="elevated">
          <div className={styles.stat}>
            <div className={styles.statValue}>0</div>
            <div className={styles.statLabel}>Clients</div>
          </div>
        </Card>
      </div>

      <Card variant="elevated">
        <h2 className={styles.sectionTitle}>Recent Activity</h2>
        <div className={styles.emptyState}>
          <p>No time entries yet. Start tracking your time!</p>
          <Button variant="primary">Start Timer</Button>
        </div>
      </Card>
    </div>
  );
}
```

Create `src/pages/Dashboard/Dashboard.module.scss`:

```scss
@import '../../styles/variables';
@import '../../styles/mixins';

.dashboard {
  display: flex;
  flex-direction: column;
  gap: $spacing-xl;
}

.header {
  @include flex-between;

  h1 {
    font-size: $font-size-3xl;
    margin-bottom: $spacing-xs;
  }

  p {
    color: $color-gray-600;
  }
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: $spacing-lg;
}

.stat {
  text-align: center;
}

.statValue {
  font-size: $font-size-3xl;
  font-weight: $font-weight-bold;
  color: $color-primary;
}

.statLabel {
  font-size: $font-size-sm;
  color: $color-gray-600;
  margin-top: $spacing-xs;
}

.sectionTitle {
  font-size: $font-size-xl;
  margin-bottom: $spacing-lg;
}

.emptyState {
  text-align: center;
  padding: $spacing-3xl;
  color: $color-gray-600;

  p {
    margin-bottom: $spacing-lg;
  }
}
```

---

## ✅ Verification

### 1. Check Design System
- [ ] Variables loaded correctly
- [ ] Mixins available
- [ ] Colors applied

### 2. Test Components
Open http://localhost:5173 and verify:
- [ ] Dashboard uses new components
- [ ] Cards render correctly
- [ ] Buttons work and have hover effects
- [ ] Responsive design works

### 3. Test Auth Pages
- [ ] Login form styled correctly
- [ ] Register form styled correctly
- [ ] Input fields have focus states
- [ ] Buttons have loading states

---

## ✅ Success Criteria

- [ ] SCSS design system complete
- [ ] All UI components created
- [ ] Components have TypeScript types
- [ ] Components are reusable
- [ ] SCSS modules working
- [ ] No styling conflicts
- [ ] Responsive design
- [ ] Professional appearance
- [ ] All imports working

---

## 📊 What We Built

```
✅ Complete SCSS Design System
   - Variables (colors, spacing, typography)
   - Mixins (flexbox, responsive, animations)
   - Reset styles
✅ 6 UI Components:
   - Button (5 variants, 3 sizes)
   - Input (with icons, errors)
   - Card (3 variants)
   - Modal (with overlay)
   - Spinner (3 sizes, 3 colors)
   - Badge (5 variants)
✅ Updated Dashboard
✅ Professional Styling
✅ Fully Typed Components
```

---

## ➡️ Next Steps

Frontend Foundation Complete! 🎉

Now you're ready for:
- **Agent 4: Shared Package** (types, schemas, utils)
- **Agent 5: Features Integration** (timer, projects, reports)

---

**UI Components complete! 🎨 Beautiful and ready to use! ✨**